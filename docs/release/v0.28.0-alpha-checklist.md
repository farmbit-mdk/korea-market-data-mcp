# v0.28.0-alpha Release Checklist

## Scope

Release goal:

```text
Clean Install Smoke Test
```

This release verifies package tarball install and startup readiness in a clean temporary directory.

It does not expand runtime behavior.

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
npm run kiwoom:quote:manual is blocked by default
git diff --check passes
npm pack --dry-run passes
npm pack executed for clean install smoke test
clean temp directory install performed or clearly documented
package bin/start execution verified or clearly documented
mock provider startup verified or clearly documented
package-based MCP config reviewed
npm publish was not performed
hosted proxy was not added
examples contain no real credentials
examples contain no account/order/trading environment variables
get_kiwoom_stock_quote public tool scope is unchanged
account/order/trading tools do not exist
fetch boundary remains in src/providers/kiwoom/transport.ts
```

## Package-Based MCP Config

Package-based examples:

```text
examples/claude-desktop.package.example.json
examples/cursor.package.example.json
```

These are for local tarball or future official npm package validation only.

Use GitHub clone setup for the current alpha unless testing package installation.

## Safety

Confirm:

```text
Real Kiwoom quote lookup remains disabled by default.
Mock provider is the recommended first setup path.
Kiwoom real local verification is explicit opt-in only.
No account access.
No orders.
No balance lookup.
No holdings lookup.
No trading.
No auto-trading.
No investment recommendations.
No centralized data redistribution proxy.
```

## Explicitly Excluded

Do not include:

```text
npm publish
hosted proxy
public real Kiwoom quote lookup by default
endpoint enabled default true
exposesPublicTool default true
public real path default true
real credentials in example files
account access
orders
balance lookup
holdings lookup
trading
auto-trading
investment recommendations
centralized data redistribution proxy
centralized credential storage
```
