# v0.7.0-alpha Release Checklist

## Scope

Release goal:

```text
Kiwoom Read-only Quote Adapter Skeleton
```

This release adds provider-side quote adapter scaffolding only. It does not add a public MCP quote tool beyond the existing registry and does not enable live Kiwoom quote lookup.

---

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
mocked quote transport tests pass
MCP tool registry remains unchanged
```

---

## Security

Before release, confirm:

```text
no credentials in git
no credentials in logs
no token values in logs or errors
fetch remains isolated to src/providers/kiwoom/transport.ts
quote errors do not include raw provider response objects
```

---

## Scope Guard

Before release, confirm:

```text
no account access
no order tools
no balance lookup
no holdings lookup
no trading
no auto-trading
no investment recommendations
no live quote, ETF, index, chart, or realtime lookup
```
