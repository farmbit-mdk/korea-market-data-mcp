# v0.26.0-alpha Release Checklist

## Scope

Release goal:

```text
Alpha Release Final Review
```

This release performs final documentation, package metadata, examples, security, public tool scope, and unsupported-scope review before first public alpha release.

It does not expand runtime behavior.

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
npm run kiwoom:quote:manual is blocked by default
git diff --check passes
README final review complete
CHANGELOG v0.26.0-alpha entry exists
SECURITY final review complete
examples final review complete
package metadata final review complete
alpha launch announcement exists
alpha final review document exists
npm publish was not performed
hosted proxy was not added
public MCP tool list reviewed
get_kiwoom_stock_quote exists and account/order/trading tools do not exist
examples contain no real credentials
examples contain no account/order/trading environment variables
fetch boundary remains in src/providers/kiwoom/transport.ts
unsupported features remain documented
```

## Public Tool Scope

Current public MCP tools:

```text
search_korean_symbol
get_stock_quote
get_kiwoom_stock_quote
get_etf_quote
get_market_index
get_daily_chart
```

Kiwoom public quote tool:

```text
get_kiwoom_stock_quote
```

The tool is guarded. Mock path is recommended first. Real Kiwoom path remains local opt-in only.

## Safety

Confirm these public alpha statements remain true:

```text
Real Kiwoom quote lookup remains disabled by default.
Mock provider is the recommended first setup path.
Kiwoom real local verification is explicit opt-in only.
No account access.
No orders.
No balance lookup.
No holdings lookup.
No trading.
No auto-trading.
No investment recommendations.
No centralized data redistribution proxy.
```

## Distribution Decision

```text
npm publish was not performed
GitHub clone/local setup remains the primary distribution path
npm publish requires a separate readiness review
hosted proxy was not added
```

## Explicitly Excluded

Do not include:

```text
npm publish
hosted proxy
public real Kiwoom quote lookup by default
endpoint enabled default true
exposesPublicTool default true
public real path default true
real credentials in example files
account access
orders
balance lookup
holdings lookup
trading
auto-trading
investment recommendations
centralized data redistribution proxy
centralized credential storage
```
