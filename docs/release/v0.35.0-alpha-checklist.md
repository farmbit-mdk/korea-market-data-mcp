# v0.35.0-alpha Checklist

Release description:

```text
Remove Mock Market Data
```

## Scope

```text
mock quote payloads removed
mock chart payloads removed
mock index payloads removed
market data context real-provider-only
resolver fixtures retained
symbol search fixtures retained
no runtime tool scope expansion
```

## Verification Commands

```text
npm run build
npm test
npm run kiwoom:setup:check
npm run kiwoom:token:manual
npm run kiwoom:quote:manual
git diff --check
npm pack --dry-run
```

## Release Checks

```text
Claude Desktop context request does not return mock quote
Claude Desktop context request does not return mock chart
Claude Desktop context request does not return mock index
Claude Desktop context request does not return data_status ok for mock data
resolver Samsung Electronics -> 005930 retained
resolver KODEX 200 -> 069500 retained
resolver KOSPI/KOSDAQ/KOSPI200 retained
no real credentials committed
```

## Explicitly Excluded

```text
No account access.
No orders.
No balance lookup.
No holdings lookup.
No trading.
No auto-trading.
No investment recommendations.
No hosted proxy.
No centralized data redistribution proxy.
```
