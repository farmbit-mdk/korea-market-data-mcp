# v0.23.0-alpha Release Checklist

## Scope

Release goal:

```text
MCP Client Setup and User Onboarding Docs
```

This release adds user-facing onboarding docs and MCP client setup examples. It does not enable public real Kiwoom quote lookup by default.

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
npm run kiwoom:quote:manual is blocked by default
README quickstart links exist
docs/getting-started/quickstart.md exists
docs/getting-started/mcp-client-setup.md exists
docs/getting-started/claude-desktop-setup.md exists
docs/getting-started/cursor-setup.md exists
docs/getting-started/troubleshooting.md exists
Claude Desktop mock JSON example exists
Claude Desktop Kiwoom local example exists
Cursor mock JSON example exists
Cursor Kiwoom local example exists
.env.example contains no real credentials
examples contain no real credentials
mock provider is recommended for first setup
real Kiwoom quote lookup remains disabled by default
account/order/balance/holdings/trading/recommendation scopes are not added
centralized data redistribution proxy is not added
fetch boundary remains in src/providers/kiwoom/transport.ts
```

## Explicitly Excluded

Do not include:

```text
public real Kiwoom quote lookup by default
endpoint enabled default true
exposesPublicTool default true
account access
orders
balance lookup
holdings lookup
trading
auto-trading
investment recommendations
centralized data redistribution proxy
real credentials in example files
```
