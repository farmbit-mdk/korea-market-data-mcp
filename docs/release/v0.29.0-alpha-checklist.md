# v0.29.0-alpha Release Checklist

## Scope

Release goal:

```text
Official npm Publish Decision
```

This release documents the decision to defer actual npm publish while recording the package ownership, access, provenance, and versioning policies required before a future publish release.

It does not expand runtime behavior.

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
npm run kiwoom:quote:manual is blocked by default
git diff --check passes
npm pack --dry-run passes
npm publish decision record written
npm publish was not performed
package name availability checked or explicitly marked unverified
npm access policy documented
versioning policy documented
SECURITY package impersonation warning confirmed
hosted proxy not added
examples contain no real credentials
examples contain no account/order/trading environment variables
get_kiwoom_stock_quote public tool scope is unchanged
account/order/trading tools do not exist
fetch boundary remains in src/providers/kiwoom/transport.ts
```

## Publish Decision

Current decision:

```text
defer publish
```

Recommended decision:

```text
defer actual npm publish until a separate final publish release
```

## Package Name Availability

Package name:

```text
korea-market-data-mcp
```

Registry check:

```text
npm view returned E404 Not Found on 2026-06-07
```

This does not reserve the package name. Recheck immediately before any future publish.

## Safety

Confirm:

```text
Real Kiwoom quote lookup remains disabled by default.
Mock provider is the recommended first setup path.
Kiwoom real local verification is explicit opt-in only.
npm publish was not performed.
No hosted proxy was added.
No account access.
No orders.
No balance lookup.
No holdings lookup.
No trading.
No auto-trading.
No investment recommendations.
No centralized data redistribution proxy.
```

## Explicitly Excluded

Do not include:

```text
npm publish
hosted proxy
public real Kiwoom quote lookup by default
endpoint enabled default true
exposesPublicTool default true
public real path default true
real credentials in example files
account access
orders
balance lookup
holdings lookup
trading
auto-trading
investment recommendations
centralized data redistribution proxy
centralized credential storage
```
