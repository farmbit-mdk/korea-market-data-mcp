# v0.27.0-alpha Release Checklist

## Scope

Release goal:

```text
npm Pack Dry Run and Publish Readiness
```

This release checks package publish readiness without performing `npm publish`.

It does not expand runtime behavior.

## Verification

Before release, confirm:

```text
npm run build passes
npm test passes
npm run kiwoom:token:manual is blocked by default
npm run kiwoom:quote:manual is blocked by default
git diff --check passes
npm pack --dry-run was executed
package contents were reviewed
.env.local is not included
real credentials are not included
README npm status is clear
distribution-readiness npm status is clear
SECURITY package distribution warning is present
npm publish was not performed
hosted proxy was not added
get_kiwoom_stock_quote public tool scope is unchanged
account/order/trading tools do not exist
fetch boundary remains in src/providers/kiwoom/transport.ts
```

## Package Metadata

Confirm:

```text
package name is korea-market-data-mcp
package version is 0.27.0-alpha
package description is read-only
package license is MIT
repository points to farmbit-mdk/korea-market-data-mcp
homepage points to the GitHub README
bugs URL points to GitHub issues
keywords describe MCP, Korean market data, Kiwoom, finance, and read-only behavior
type is module
main is dist/index.js
bin maps korea-market-data-mcp to dist/index.js
files include dist, docs, examples, .env.example, CHANGELOG.md, README.md, SECURITY.md, and LICENSE
engines require Node.js 20 or newer
```

## Package Contents Review

Expected included paths:

```text
dist/**
docs/**
examples/**
package.json
README.md
LICENSE
SECURITY.md
CHANGELOG.md
.env.example
```

Forbidden contents:

```text
.env.local
real credentials
access tokens
authorization headers
private scratch files
logs
account numbers
order identifiers
balance data
holdings data
trading data
centralized proxy configuration
```

## Safety

Confirm:

```text
Real Kiwoom quote lookup remains disabled by default.
Mock provider is the recommended first setup path.
Kiwoom real local verification is explicit opt-in only.
No account access.
No orders.
No balance lookup.
No holdings lookup.
No trading.
No auto-trading.
No investment recommendations.
No centralized data redistribution proxy.
```

## Explicitly Excluded

Do not include:

```text
npm publish
hosted proxy
public real Kiwoom quote lookup by default
endpoint enabled default true
exposesPublicTool default true
public real path default true
real credentials in example files
account access
orders
balance lookup
holdings lookup
trading
auto-trading
investment recommendations
centralized data redistribution proxy
centralized credential storage
```
